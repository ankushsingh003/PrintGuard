services:
  - type: web
    name: printguard-ai
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    port: 10000
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: "3.10"
